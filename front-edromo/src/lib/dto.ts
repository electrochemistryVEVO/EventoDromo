export type EntradaDTO = {
    id: number;
    nombreEvento: string;
    nombreLocal: string;
    precioEntrada: number;
    cantidadEntradas: number;
    tipoEntrada: string;
    imagenURL: string;
}